export const fetchProperties = async (params) => {
    try {
        console.log(params);
    } catch (e) {
        console.log(e);
    }
}

export const fetchPropertyDetail = async (id) => {
    try {
        console.log(id);
    } catch (e) {
        console.log(e);
    }
}